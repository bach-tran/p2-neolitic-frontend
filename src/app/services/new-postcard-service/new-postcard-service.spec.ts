import { NewPostcardService } from './new-postcard-service';

describe('NewPostcardService', () => {
  it('should create an instance', () => {
    expect(new NewPostcardService()).toBeTruthy();
  });
});
