import { NewCommunityService } from './new-community-service';

describe('NewCommunityService', () => {
  it('should create an instance', () => {
    expect(new NewCommunityService()).toBeTruthy();
  });
});
